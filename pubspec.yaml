name: simple_sidebar
description: A very simple sidebar with Icons, Texts, Header and smoothing. Easy to implement and easy to use
version: 0.0.7
homepage: https://github.com/felixkriechbaum/SimpleSidebar

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.2.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
